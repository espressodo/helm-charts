{{- if and .Values.ingress.enabled .Values.espresso.scim.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: espresso-scim
  labels:
    {{- include "espresso.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className | quote }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          - path: /scim
            pathType: Prefix
            backend:
              service:
                name: espresso-scim
                port:
                  number: 8109
{{- end }}
